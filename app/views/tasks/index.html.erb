<div>
  <h1>My Tasks</h1>
</div>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<p></p>
<%= link_to "Create a new task", new_task_path %>
